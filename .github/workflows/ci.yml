name: Build & Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
env:
  LOGGER_LAYER_URI: github:DCC-BS/nuxt-layers/pino-logger
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: DCC-BS/ci-workflows/actions/node-bun-biome-playwright@dd91f584deec446a76ab9f3b1afbeafa9a6c244b # v9
        with:
          node_version: "24.x"
          working_directory: "."
          run_biome: "true"
          biome_version: "2.4.15"
          run_playwright: "true"
          install_method: "bun"
          build_command: "DUMMY=true bun run build"
          test_command: "bun run test:e2e"
          artifact_name: "playwright-report"
          artifact_retention_days: 30
